<!DOCTYPE html>
<html lang="en">
<head>
  <title>Portfolio</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Rails Meta Tags -->
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Stylesheets -->
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="<%= asset_path 'buttons.css' %>" rel="stylesheet">
  <link href="<%= asset_path 'footer.css' %>" rel="stylesheet">
  <link href="<%= asset_path 'menu.css' %>" rel="stylesheet">

  <!-- JavaScript -->
  <%= javascript_importmap_tags %>
  <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>

</head>
<body class="bg-gradient-to-b from-gray-50 to-gray-100 text-gray-900 min-h-screen flex flex-col">

  <!-- Navbar -->
  <%= render 'shared/navbar' %>

  <!-- Main Content -->
  <main class="flex-grow pt-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <%= yield %>
    </div>
  </main>

  <!-- Footer -->
  <footer>
    <%= render 'shared/footer' %>
  </footer>

  <!-- External JavaScript -->
    <script>
    function openProject(link) {
      window.open(link, '_blank');
    }
  </script>

</body>
</html>
